import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import Example from './Example.tsx';
import * as ResourceNode from './ResourceNode.stories.tsx';

<Meta of={ResourceNode} title="ResourceNode" />

# ResourceNode

ResourceNode component for different types of resources.

#### Example

<Canvas of={ResourceNode.Container} />

#### Arguments

- **resource** _`Resource`_ - The resource to display.
